[gd_scene load_steps=4 format=2]

[ext_resource path="res://Assets/ladder.png" type="Texture" id=1]

[sub_resource type="GDScript" id=1]
script/source = "extends Node2D

export(bool) var is_top = false
export(bool) var is_bottom = false

# Declare member variables here. Examples:
# var a = 2
# var b = \"text\"


# Called when the node enters the scene tree for the first time.
func _ready():
	if not is_top:
		remove_child($LadderTop)
	if not is_bottom:
		remove_child($LadderBottom)
		



# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
#	pass
"

[sub_resource type="RectangleShape2D" id=2]
extents = Vector2( 8, 10 )

[node name="Ladder" type="Node2D"]
position = Vector2( 0, -4 )
script = SubResource( 1 )

[node name="Sprite" type="Sprite" parent="."]
texture = ExtResource( 1 )

[node name="LadderArea" type="Area2D" parent="."]
scale = Vector2( 0.5, 1 )

[node name="CollisionShape2D" type="CollisionShape2D" parent="LadderArea"]
scale = Vector2( 0.4, 0.4 )
shape = SubResource( 2 )
